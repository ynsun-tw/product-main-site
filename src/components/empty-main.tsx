/** Shared shell for placeholder routes — same vertical rhythm as the home pricing page */
export function EmptyMain() {
  return (
    <main
      className="mx-auto w-full max-w-[1200px] flex-1 px-6 pb-32 pt-24 font-sans"
      aria-label="Page content"
    />
  );
}
